{
  "name": "sql-visual-debugger",
  "displayName": "SQL Visual Debugger",
  "description": "Step through supported MySQL SELECT queries clause by clause and inspect intermediate results inside VS Code.",
  "icon": "icon.png",
  "version": "0.1.1",
  "publisher": "sql-visual-debugger",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ArielTurgeman/sql-visual-debugger.git"
  },
  "homepage": "https://github.com/ArielTurgeman/sql-visual-debugger",
  "bugs": {
    "url": "https://github.com/ArielTurgeman/sql-visual-debugger/issues"
  },
  "categories": [
    "Debuggers",
    "Visualization"
  ],
  "keywords": [
    "sql",
    "mysql",
    "debugger",
    "query",
    "database",
    "visualizer",
    "teaching",
    "education",
    "analysis",
    "learning"
  ],
  "engines": {
    "vscode": "^1.80.0"
  },
  "activationEvents": [
    "onLanguage:sql"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "sqlDebugger.debugQuery",
        "title": "SQL Debugger: Debug Query"
      },
      {
        "command": "sqlDebugger.configureConnection",
        "title": "SQL Debugger: Configure Connection"
      },
      {
        "command": "sqlDebugger.changeConnection",
        "title": "SQL Debugger: Switch Database"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "sqlDebugger.debugQuery",
          "when": "editorLangId == sql",
          "group": "navigation"
        }
      ],
      "commandPalette": [
        {
          "command": "sqlDebugger.debugQuery"
        },
        {
          "command": "sqlDebugger.configureConnection"
        },
        {
          "command": "sqlDebugger.changeConnection"
        }
      ]
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "test:unit": "node test/run-tests.js",
    "test": "npm run compile && npm run test:unit"
  },
  "dependencies": {
    "mysql2": "^3.9.7"
  },
  "devDependencies": {
    "@types/node": "^20.14.15",
    "@types/vscode": "^1.80.0",
    "typescript": "^5.5.4"
  }
}
